<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Remove the old custom Tool Borrow tab from user form.
         The Equipment Manager group now appears automatically in the
         Access Rights tab via Odoo's native sel_groups mechanism. -->
    <delete model="ir.ui.view" search="[('key', '=', 'tool_borrow.res_users_view_form_tool_borrow')]"/>
</odoo>
